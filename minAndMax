function minMax(arr) {
	let output = [];
	let min = arr[0];
	let max = arr[arr.length-1];
	for(let i=1; i<arr.length; i++){
		if(min > arr[i]){
			min = arr[i];
		}
		if(max < arr[i]){
			max = arr[i];
		}
	}
	output = [min, max];
	return output;
}